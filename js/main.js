(function (g, d, $, u){
    var
        init = function() {
            var
                title = $('title').text() + ' :: Przemysław SID Szelenberger';

            $('title').text(title);
        };
    $(init);
}(window, document, jQuery));
    